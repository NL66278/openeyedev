---
odoo:
  defaults:
    depth: ${WAFT_DEPTH_DEFAULT}
  remotes:
    origin: https://github.com/odoo/odoo.git
  target: origin ${ODOO_VERSION}
  merges:
    - origin ${ODOO_VERSION}


OCA/l10n-netherlands:
  defaults:
    depth: ${WAFT_DEPTH_DEFAULT}
  remotes:
    origin: https://github.com/OCA/l10n-netherlands.git
  target: origin ${ODOO_VERSION}
  merges:
    - origin ${ODOO_VERSION}

OCA/server-tools:
  defaults:
    depth: ${WAFT_DEPTH_DEFAULT}
  remotes:
    origin: https://github.com/OCA/server-tools.git
  target: origin ${ODOO_VERSION}
  merges:
    - origin ${ODOO_VERSION}

OCA/web:
  defaults:
    depth: ${WAFT_DEPTH_DEFAULT}
  remotes:
    origin: https://github.com/OCA/web.git
  target: origin ${ODOO_VERSION}
  merges:
    - origin ${ODOO_VERSION}
